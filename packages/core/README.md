# core

Shared SDLC primitives used across ingestion/inference/evaluation/experimentation.

This package provides:

- stable hashing + canonical JSON helpers
- typed IDs and run-id helpers
- shared artifact/run reference models
- file-backed artifact/run stores
